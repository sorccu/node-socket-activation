{
  "name": "socket-activation",
  "version": "3.0.0",
  "description": "Native socket activation for launchd, systemd et al.",
  "keywords": [
    "launch_activate_socket",
    "launchd",
    "systemd",
    "socket activation"
  ],
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/sorccu/node-socket-activation.git"
  },
  "main": "./index",
  "scripts": {
    "install": "node ./prebuilt-bindings install",
    "prebuilt-bindings": "node ./prebuilt-bindings"
  },
  "dependencies": {
    "bindings": "^1.2.1",
    "nan": "^2.4.0",
    "prebuilt-bindings": "^1.0.2"
  }
}
